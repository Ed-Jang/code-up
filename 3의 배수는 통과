#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

//1부터 입력한 정수까지 1씩 증가시켜 출력
//3의 배수는 미출력

int main(void) {
	int num;
	scanf("%d", &num);
	for (int i = 0; i <= num; i++) {
		if (i % 3 == 0) {
			continue;
		}
		printf("%d ", i);
	}
	return 0;
}
