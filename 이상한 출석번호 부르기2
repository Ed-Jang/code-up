#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h> 

int main(void) {
	int n, i;
	int a[10000] = {0,};
	scanf("%d", &n); //n개수만큼 입력받기
	for (i = 1; i <= n; i++) {//개수만큼 입력받기
		scanf("%d", &a[i]);//읽어서 순서대로 배열에 넣는다
	}

	for (i = n; i >= 1; i--) {
		printf("%d ", a[i]);//i번 배열에 저장되어 있는 값 출력하기
	}
}
