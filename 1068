#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int a;
	scanf("%d", &a);
	if (90 <= a) {
		printf("%s", "A");
	}
	else if ( 70 <= a) {
		printf("%s", "B");
	}
	else if ( 40 <= a) {
		printf("%s", "C");
	}
	else{
		printf("%s", "D");
	}
	return 0;
	}
