#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void gugudan(int a);

int main(void) {
	int a;
	printf("input : ");
	scanf("%d", &a);
	gugudan(a);
	return 0;
}

void gugudan(int a) {
	for (int i = 1;i < 10; i++) {
		printf("%d * %d = %d\n", a, i, a * i);
	}
}
