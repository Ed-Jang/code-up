#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int arr[9];
	int i = 0, count = 0;
	int max = 0;
	for (int i = 0; i < 9; i++) {
	scanf("%d", &arr[i]);
		if (arr[i] > max) {
			max = arr[i];
			count = i + 1;
		}
	}
	printf("%d\n", max);
	printf("%d\n", count);
	return 0;
}
