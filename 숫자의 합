#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h> 

int main(void) {
	int n = 0, result = 0;
	char num[100] = { 0, };

	scanf("%d", &n);
	scanf("%s", num);

	for (int i = 0; i < n; i++) {
		result += num[i]-48;
	}
	printf("%d", result);

	return 0;
}

