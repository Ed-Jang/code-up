#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int num;
	scanf("%d", &num); //1~9 입력
	for (int i = 1; i <= num; i++) {
		if (i % 3 == 0) printf("X ");
		else printf("%d ", i);
	}
	return 0;
}
