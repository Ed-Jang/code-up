#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	long long h, b, c, s;//헤르츠,체크결과,스테레오,녹음시간
	double result;
	scanf("%lld %lld %lld %lld", &h, &b, &c, &s);
	result = h * b * c * s;
	printf("%.1f MB", result/(8*1024*1024));
	return 0;
}
